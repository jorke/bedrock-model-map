[project]
name = "bedrock-model-map"
version = "0.1.0"
description = "Generate a capability map of all Amazon Bedrock foundation models from AWS docs model cards."
requires-python = ">=3.9"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
